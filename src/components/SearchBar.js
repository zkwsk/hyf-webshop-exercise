import React from 'react';

const SearchBar = () => {
	return (
		<div className="well">
			<span>
				<input type="text" />
			</span>
			<span>
				<button>Search</button>
			</span>
		</div>
	);
};

export default SearchBar;
